codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: 70%
        threshold: 5%
    patch:
      default:
        target: 60%

comment:
  layout: "header, diff, flags, components"
  behavior: default
  require_changes: true
